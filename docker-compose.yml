version: '2'
services:
    test:
        image: php:7.4-apache-buster
        env_file:
          - local.env
        volumes:
            - ./:/data/
        working_dir: /data
        command: vendor/bin/behat
