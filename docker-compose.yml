version: '2'
services:
    test:
        image: silintl/php7:7.2
        env_file:
          - local.env
        volumes:
            - ./:/data/
        working_dir: /data
        command: vendor/bin/behat
